{
  "Redis": {
    "ConnectionString": "localhost:6379,syncTimeout=60000,asyncTimeout=60000,connectTimeout=10000,keepAlive=10,configCheckSeconds=0,abortConnect=false",
    "Streams": [
      {
        "StreamName": "telemetry",
        "GroupName": "avg"
      },
      {
        "StreamName": "orders",
        "GroupName": "processing"
      }
    ]
  }
}